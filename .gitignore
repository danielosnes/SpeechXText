# --- Logs ---
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# --- Runtime / reports ---
pids
*.pid
*.seed
*.pid.lock
report.*.json

# --- Coverage / build artifacts ---
coverage
*.lcov
.nyc_output
lib-cov
build/Release

# --- Package managers ---
node_modules/
jspm_packages/
web_modules/

# --- Caches ---
.cache
.parcel-cache
.eslintcache
.stylelintcache
.npm
.node_repl_history
.yarn-integrity
.svelte-kit/
.docusaurus

# --- Framework outputs (only if you ever use them) ---
.next
out
.nuxt
dist
.vuepress/dist
**/.vitepress/dist
**/.vitepress/cache

# --- Tooling ---
.vscode-test
.fusebox/
.serverless/
.dynamodb/
.firebase/
.tern-port
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# --- OS junk ---
.DS_Store
Thumbs.db

# --- Environment & secrets (KEEP PRIVATE) ---
.env
.env.*
!.env.example
dialogflow-key.json
# If you prefer a pattern:
# service-account*.json
# *-key.json

# --- Keep JSON files that are part of source control ---
# DO NOT ignore all JSON files; we need package.json & package-lock.json
